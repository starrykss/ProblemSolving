#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL);
    cout.tie(NULL);

    string name;
    cin >> name;

    cout << name << "??!" << endl;    // cout << name + "??!" << endl; is also possible.

    return 0;
}